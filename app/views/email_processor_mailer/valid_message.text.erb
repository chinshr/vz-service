You got it! We are processing your request with:

<% @message.attachments.each do |upload| -%>
  <% if upload.has_s3_source_url? -%>
  <%= "#{upload.file_name} (#{upload.file_type})" %>
  <% else -%>
  <%= "#{upload.source_url}" %>
  <% end -%>
<% end -%>

We will send you an update shortly once we are done.
