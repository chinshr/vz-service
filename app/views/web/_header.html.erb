<header>
  <div class="container">
    <nav class="navbar navbar-default navbar-fixed-top" id="navigation" role="navigation">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a id="logo" class="navbar-brand" href="/">VOYZ.ES</a>
          <% if content_for?(:navbar_tag) %>
            <%= content_for(:navbar_tag) %>
          <% else %>
          <a class="navbar-tag" href="/">Record your Life</a>
          <% end %>
        </div>
        
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <% if current_user %>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= image_tag(avatar_url(current_user), class: "avatar", style: "background-color: #{current_user.css_rgb}") %>
                 <span>Account Settings </span><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><a href="<%= web_dashboard_path %>"><span class="badge badge-warning pull-right">3</span>Dashboard</a></li>
                <li><a href="<%= web_account_profile_path %>">User Profile</a></li>
                <li><a href="<%= web_account_settings_path %>">Account Settings</a></li>
                <li><a href="<%= web_account_billing_path %>">Billing Information</a></li>
                <li class="divider"></li>
                <li class="logout">
                  <%= link_to "Logout", destroy_user_session_path, :method => :delete, :class => "btn btn-small btn-danger" %>
                </li>
              </ul>
            </li><!-- dropdown -->
          </ul>
          <% else %>
          <% if false %>
          <ul class="nav navbar-nav">
            <li class="active"><a href="#">Overview</a></li>
            <li><a href="#">Getting Started</a></li>
            <li><a href="#">Pricing</a></li>
          </ul>
          <% end %>
          <div id="login-buttons" class="btn-group btn-group-horizontal pull-right">
            <a href="<%= new_user_session_path %>" class="btn btn-small btn-flat-plain">Login</a>
          </div>
        <% end %>
        </div><!-- collapse navbar-collapse -->
      </div><!-- container-fluid -->
    </nav>
  </div><!-- end container -->
</header>

